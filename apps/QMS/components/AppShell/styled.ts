import styled from '@emotion/styled'

export const StyledAppShell = styled.section`
  position: fixed;
  top: 50px;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 30;

  .qms-layout {
    height: 100%;
  }
`
