import styled from '@emotion/styled'

export const StyledLoginPage = styled.div`
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  display: flex;
  z-index: 90;
  justify-content: center;
  align-items: center;

  form {
    width: 400px;
    margin: 40px 0;
  }
`
